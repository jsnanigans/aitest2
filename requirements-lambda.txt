# Minimal requirements for AWS Lambda deployment
# Optimized for smallest possible package size

# Core mathematical library (required for Kalman filter)
numpy==1.26.4  # Pinned for Lambda compatibility

# Data validation (required for API models)
pydantic==2.11.9

# Kalman filter implementation
pykalman==0.9.5  # Older version is smaller and sufficient

# IMPORTANT: Do NOT include:
# - boto3: Provided by Lambda runtime
# - tomllib: Built-in to Python 3.11+
# - matplotlib/plotly/pandas: Not needed for Lambda
# - Any visualization libraries
# - Any development/testing tools

# Total package size with these deps: ~15MB (well under Lambda's 250MB limit)
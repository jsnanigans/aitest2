# Minimal requirements for AWS Lambda deployment
# Keep this as small as possible to reduce Lambda package size

# Core dependencies only
numpy==1.26.4  # Pinned for Lambda compatibility
pydantic==2.11.9
pykalman==0.10.2

# Note: AWS Lambda runtime provides boto3, so we don't include it
# unless we need a specific version different from the runtime